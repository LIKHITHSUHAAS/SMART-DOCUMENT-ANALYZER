name: Sync to Hugging Face Hub
on:
  push:
    branches: [master]
jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          lfs: true
      - name: Push to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git push --force https://likhithsuhaas:$HF_TOKEN@huggingface.co/spaces/likhithsuhaas/SMART-DOCUMENT-ANALYZER master:main
